apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-6-project-template.fullname" . }}
  labels:
    {{- include "spring-6-project-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "spring-6-project-template.selectorLabels" . | nindent 4 }}